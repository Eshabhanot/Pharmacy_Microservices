server:
  port: 8081
 
spring:
  datasource:
      url: jdbc:mysql://localhost:3306/authDb
      username: root
      password:

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
